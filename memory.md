# Memory — MangoAI

## État actuel (2026-06-13 — Phase Ultime Jalon D LIVRÉ)
- **MVP + roadmap concurrence (5/5) + refonte UI + boucle d'apprentissage Hermes (5/5) + compression de contexte + raisonnement analytique + robustesse des magasins + mode vision en boucle fermée + Phase Ultime A/B/C/D** : tout est FAIT, testé de bout en bout — détail dans `statut.md` et `changelog.md`
- **Phase Ultime COMPLÈTE (A→D, 2026-06-13)** : MangoAI est désormais model-agnostic — l'Élève local **Qwen2.5-coder:7b** (Ollama, sélecteur « 🎓 Élève local ») traite la routine à coût zéro, Claude reste l'escalade + scribe d'axiomes. Voir les règles Jalon D plus bas + le dashboard de compagnonnage (panneau 📊) et l'Audit Scan (`npx tsx src/audit-scan.ts [--ablate]`)
- Lancement : `npm run start` dans `server/` (port 3000) + `npm run dev` dans `ui/` (port 5173) → ouvrir http://localhost:5173
- Projet de test : `workspace/test-pipeline/` (landing Bella Napoli — sert de banc d'essai à toutes les features)
- **Travail à venir** : voir `statut.md` § « 🔜 Aussi à faire » — c'est la seule source de vérité du backlog ; et TOUJOURS montrer à l'utilisateur le tableau « 💡 Idées en attente » (tout en haut de `statut.md`) au démarrage d'une session, avant de coder quoi que ce soit
- **Plan de formation & développement** : `PLAN-FORMATION.md` — 26 idées, 6 phases, timing, checkboxes, guides pour les projets formation. À relire au démarrage de session et à tenir à jour. **Phase 1 FAITE : idées 12 (MVP/Élite) ✅, 16 (GitHub) ✅, 17 (Supabase) ✅. Phases 1 ET 2 COMPLÈTES côté code : 12 (MVP/Élite), 16 (GitHub), 17 (Supabase), 10 (Flywheel), 8+6 (blueprints), 9+11 (Mango Plan + moodboard). Prochaine étape : à définir avec l'utilisateur (il a « un plan plus ambitieux »). Phase 3 (formation) mise de côté à sa demande. GitHub ✅ et Supabase ✅ VALIDÉS EN LIVE (2026-06-13) : push GitHub réel réussi ; app `todo-supabase` avec persistance cloud réelle confirmée. Phase 1 100 % validée. Clés dans `.credentials/`**
- **Deux types de travail** : 🔧 Type A = on améliore MangoAI ensemble ici dans Claude Code ; 🧪 Type B = l'utilisateur construit un projet dans MangoAI (http://localhost:5173) pour se former à l'IA
- **Veille mensuelle** : `VEILLE-MENSUELLE.md` — checklist à ouvrir le 13 de chaque mois

## Cap produit — usage en 2 phases (acté 2026-06-13)
MangoAI est et reste un **outil perso local-first**. La trajectoire prévue par l'utilisateur :
- **Phase A — interne (en cours)** : finir TOUTES les idées de la feuille de route, PUIS s'en servir personnellement pour produire de vrais projets déjà en tête. Mono-poste, mono-utilisateur, pas de déploiement. → Les correctifs « avant prod » (CORS ouvert, auth des endpoints, rate-limiting, tests HTTP d'intégration) sont **HORS SUJET tant qu'on est en Phase A** ; ne pas les proposer comme urgents.
- **Phase B — bêta proche (plus tard)** : exporter l'app sur un AUTRE PC pour que des bêta-testeurs proches l'évaluent, **sans accès au code source de l'agent**. C'est un problème de **packaging/distribution** (compiler `server/` en JS/binaire — `bun build --compile`, `pkg`… ; livrer le front buildé + serveur packagé sans les `.ts` ; obfuscation légère), PAS de sécurité applicative. À la frontière A→B, réactiver les correctifs « prod » ci-dessus (un tiers tape alors sur l'app). Voir idée 29 dans `statut.md`.
- **Audit de code (2026-06-13)** : verdict « cœur sain, bien défendu » (contrat → exécuteur, path traversal bloqué 3 couches, secrets jamais loggés). Seule action urgente indépendante des phases : le **token GitHub** (`server/.env:5`) a été exposé en clair pendant l'audit → à révoquer/regénérer (fine-grained, repos cibles only). Dette « prod » = à traiter en Phase B uniquement.

## Boucle d'apprentissage (architecture Hermes transposée)
- **3 magasins**, tous dans `workspace/` (git-ignoré, hors zip, survivent au rollback) :
  - `workspace/<projet>/.memory.md` — faits du projet (design, conventions) ; snapshot gelé injecté au system prompt à chaque tour (`server/src/memory.ts`)
  - `workspace/.user-profile.md` — préférences inter-projets de l'utilisateur (ton, typo)
  - `workspace/.skills/<nom-classe>/SKILL.md` — savoir-faire réutilisable ; divulgation progressive : seules les métadonnées au prompt, lecture à la demande (`server/src/skills.ts`)
- **Qui écrit ?** La revue en arrière-plan (`server/src/review.ts`) : agent haiku silencieux, fire-and-forget après chaque tour livré sans erreur, cwd = workspace, outils Read/Write/Edit, verrou anti-empilement, pas de récursion. L'agent principal ne cure PAS (sauf demande explicite de l'utilisateur)
- **Subagents** : agent `builder` via `options.agents` du SDK — outils fichiers seulement (pas de Bash → pas de conflit npm/preview entre builders), délégation réservée aux gros chantiers à volets indépendants
- **Panneau « 🧠 Mémoire »** dans le header : `GET /api/knowledge/:name` + `ui/src/components/Knowledge.jsx` (fetch à l'ouverture du menu = toujours frais)
- **Panneau « Aide » (idée 5, 2026-06-14)** : `ui/src/components/Guide.jsx` — guide utilisateur STATIQUE (zéro backend/dépendance) dans un dropdown header (icône `HelpCircle`), 6 groupes (Démarrer / Cerveau & mode / Visuel / Itérer en sécurité / Livrer / Suivre), chaque entrée = quoi + quand. Sert aussi de doc pour les bêta-testeurs Phase B. Tenir à jour quand une feature user-facing change
- **Compression de contexte** (`server/src/compaction.ts`) : le SDK détient l'historique → on déclenche son `/compact` en arrière-plan (haiku) quand `contextTokens` (dernier appel API, mesuré dans `agent.ts`) dépasse 70 % de la fenêtre ; succès = présence d'un `compact_boundary` (un result « success » seul peut signifier « Not enough messages ») ; `interruptCompaction()` au début de `/api/chat` évite toute collision de session ; base incompressible ~33k tokens (system prompt + outils + mémoire)
- Clone d'étude Hermes : `C:\Users\PC-DELL\hermes-agent-study` — à NE PAS committer

## Coffre local d'identifiants
- **`.credentials/`** (racine, git-ignoré) : pense-bête local des identifiants/clés des services (Supabase, etc.). Y aller lire quand l'utilisateur dit « les clés sont dans .credentials ». `supabase.md` (URL + clé publiable `VITE_SUPABASE_*`), `github.md` (pointeur — le vrai token vit dans `server/.env`). NE JAMAIS recopier ces secrets dans un fichier tracké ni dans une app frontend (clé secrète Supabase = backend only)

## Règles spécifiques au projet
- **Langue** : réponses en français, code/commentaires en anglais
- **Agent SDK** : contrairement à la doc, le SDK v0.3.x **réutilise le login Claude Code local** — aucune `ANTHROPIC_API_KEY` nécessaire (vérifié le 2026-06-11). `.env` optionnel pour forcer une clé API
- **Modèle par défaut** : `sonnet` ; la revue en arrière-plan tourne toujours en `haiku` (~$0.02-0.08/revue)
- **Thinking** : `thinking: { type: "adaptive", display: "summarized" }` pour opus/sonnet (jamais `budget_tokens` — déprécié sur les modèles 4.6+) ; les blocs thinking arrivent comme `block.type === "thinking"` dans les messages assistant du SDK
- **Multi-tours** : capturer `session_id` du message `type: "result"` puis passer `options.resume` aux tours suivants
- **cwd absolu obligatoire** pour `query()` (les sessions sont stockées par cwd encodé dans ~/.claude/projects/)
- **Permissions agent principal** : `permissionMode: "acceptEdits"`, `allowedTools: ["Read","Write","Edit","Bash","Glob","Grep","Agent"]` + `agents: { builder }`
- **Interdits à l'agent** (system prompt) : lancer le dev server, toucher au script error-relay, lancer git (le backend versionne chaque tour)
- Un seul projet généré actif à la fois (un seul dev server preview sur le port 5174)
- Export zip : archiver v8 est ESM pur, importer `{ ZipArchive }` (pas de default export)
- **Robustesse** : les magasins JSON (`sessions.json`, `.chat-history.json`) passent par `safe-io.ts` (écriture atomique temp+rename) et sont validés au chargement (entrées invalides filtrées, jamais d'exception) ; les frontmatters `SKILL.md` sont plafonnés (80/240 car.) et une skill illisible est ignorée ; filet `unhandledRejection`/`uncaughtException` dans `index.ts`. Isolation : tous les outils/agents tournent en subprocess du SDK — pas de module `tools/` local, rien d'in-process à isoler
- **Métriques** : chaque tour ajoute une ligne à `workspace/.metrics.jsonl` (`server/src/metrics.ts`, champs dont `mode`) — ne pas supprimer ce fichier, c'est l'historique d'apprentissage pour l'audit du 2026-06-22 (fin de Fable, idées 13/14 du tableau). Champs Jalon D : `resolvedBy` (eleve|maitre|none), `attempts`, `projectType` (inféré par `inferProjectType`). Lecture via `readMetrics()` (tolérante) ; `GET /api/metrics` renvoie `{rows, insights}` où `insights = computeInsights()` (`metrics-insights.ts`) : rendement 1er tour, souveraineté estimée, émancipation, clapet, par type. Dashboard `Metrics.jsx` (charts CSS, zéro dépendance). **Idée 21 (2026-06-14)** : `computeInsights` expose en plus `weekly` (coût/tours par semaine ISO via `weekStart`, pur) et `costDrivers` (par type : coût/tours/snapshots/durée moyens) — vues d'audit pour le 22/06, rendues en barres+table dans `Metrics.jsx`, toujours zéro-dépendance (PAS recharts malgré l'idée — cohérence + packaging Phase B). Sur TOUS les tours, pas seulement l'Élève
- **Mango Plan + moodboard (idées 9+11, Élite uniquement)** : `server/src/plan.ts` → `PLAN_RULES` (concevoir avant coder : ≤3 questions, `plan.md`, validation, puis code) + `MOODBOARD_RULES` (recherche des leaders → règles de design dans plan.md). Gated par `isElite` dans `agent.ts` ; les outils `WebSearch`/`WebFetch` ne sont dans `allowedTools` QU'en Élite. MVP = pas de plan, pas de web (rapide/économe). `plan.md` = doc projet versionné. Capture visuelle externe (Playwright) = v2 reportée. **Idée 7 (2026-06-14)** : `MOODBOARD_RULES` extrait maintenant DEUX choses des 3-5 leaders — règles de design ET **architecture d'information contextuelle** (pages/sections/nav du domaine) → personnalise le blueprint générique (idée 8) et pilote la target tree de `plan.md` ; `PLAN_RULES` = blueprint comme squelette à customiser. Gating prouvé par `test-scenario.ts`. La capture visuelle Playwright des leaders reste reportée (fragile/ToS)
- **Blueprints par type (idées 6+8)** : `server/src/blueprints.ts` → `BLUEPRINTS_RULES` injecté au system prompt (tous modes, après `SYSTEM_APPEND`). Catalogue compact de 6 types (vitrine, web app, dashboard, jeu 2D canvas, slides, agent IA `@anthropic-ai/sdk`) : stack optimale + arborescence type, profondeur « structurée mais souple » (l'agent peut s'écarter). Stacks limitées au runtime Vite-SPA du preview. Tenir concis (idée 13 surveille le prompt)
- **Knowledge Flywheel (idée 10)** : 4ᵉ magasin `server/src/axioms.ts` → `workspace/.axioms.md` (inter-projets, git-ignoré, plafond 3000 car.). Registre d'axiomes universels (règle abstraite indépendante du langage/projet), distinct des skills (code/how-to). Curé par `review.ts` (haiku), injecté au system prompt de l'agent comme **défaut écrasable**. Format `AXIOME-[CAT]-[NN] (maturité: candidat|confirmé · vu: date)` + Contexte/Piège/Règle d'or. Garde-fous dans le prompt de revue : candidat à la 1ʳᵉ vue, falsifiable, plafonné. Exposé dans `/api/knowledge` + panneau Mémoire. ⚠ haiku tend à sur-promouvoir en « confirmé » — consigne renforcée mais à surveiller
- **Figma design-to-code (idée 25, 2026-06-14) — NATIF, pas le MCP officiel** : analyse natif-d'abord (même réflexe qu'idée 27). Le serveur MCP Figma officiel exigerait Figma desktop + fichier ouvert ; superflu car l'agent **voit déjà les PNG** (`vision.ts`) et l'**API REST Figma** donne les deux moitiés du vrai problème. `server/src/figma.ts` = outil MCP **in-process** (`createSdkMcpServer`, calqué sur `vision.ts`) : `mcp__figma__import({url})` → `parseFigmaUrl` (fileKey + node-id « 12-34 »→« 12:34 ») → `/v1/files/:key/nodes` (arbre) + `/v1/images/:key?format=png&scale=` (PNG rendu, `pickScale` borne la taille) → renvoie **bloc image + tokens distillés** (`distillDesign` : palette/typo/composants/outline, pur & testé). Auth `FIGMA_TOKEN` dans `.env` (header `X-Figma-Token`), `figmaConfigured()`. Branché dans `agent.ts` : `mcpServers: { vision, figma }` + `mcp__figma__import` dans `allowedTools` + libellé « Figma ». Bloc prompt `FIGMA_RULES` dans `scenario.ts` (les 2 scénarios — « si lien figma.com, appelle l'outil EN PREMIER »). Test `test-figma.ts` 16/16. ⚠ e2e live non prouvé (besoin `FIGMA_TOKEN` + vrai fichier). ⚠ Variables Figma publiées = Enterprise-only en REST ; fills/fonts/espacements toujours dispo dans l'arbre
- **Déploiement multi-cibles (idée 18, 2026-06-14)** : `server/src/deploy.ts` = registre de providers. `DeployTarget` = `cloudflare|vercel|netlify`, `DEPLOY_TARGETS`, `isDeployTarget` (garde de route), `deployProject(dir,name,target="cloudflare")`. Chaque provider : `buildDist` commun (`npm run build` → vérif `dist/index.html`) puis CLI dédié (`npx wrangler pages` / `npx vercel deploy <dist> --prod --yes` / `npx netlify-cli deploy --prod --dir <dist> --json`). Auth = **login CLI one-time** dans `server/` (`npx wrangler|vercel|netlify login`) — PAS de token en `.env` (cohérent posture sécurité ; `VERCEL_TOKEN`/`NETLIFY_AUTH_TOKEN` honorés si présents mais jamais requis). `friendly(err,target)` mappe « non connecté » → message actionnable par cible ; `extractFirstUrl` (pur, testé) parse l'URL de la sortie CLI. Route `POST /api/deploy/:name` lit `{target}` (défaut cloudflare). UI : `Header.jsx` bouton « Publier » = `Dropdown` (nouvelle prop `buttonClass` pour garder le style accent) listant `DEPLOY_TARGETS`, `App.jsx` `deploy(target)` envoie `{target}`. Test `test-deploy.ts` (pur 14/14). ⚠ e2e live non prouvé tant que l'utilisateur n'a pas fait le login one-time Vercel/Netlify (Cloudflare idem). Netlify exige un site lié (`npx netlify init`) — message dédié si absent
- **GitHub natif (idée 16)** : `server/src/github.ts` — `GITHUB_TOKEN` (scope `repo`) dans `server/.env`, login lu via API `/user`, repo créé si absent (`POST /user/repos`, privé par défaut), force-push de l'historique git du projet via URL inline authentifiée (token jamais écrit dans `.git/config`, remote `origin` propre). `POST /api/github/:name`, `githubEnabled` dans `/api/projects` (bouton masqué sans token). Lucide n'exporte plus `Github` → icône `GitFork`. **Push réel VALIDÉ (2026-06-13)** : repo privé créé, remote propre sans token, aucun secret versionné
- **Supabase (idée 17)** : bloc `SUPABASE_RULES` (concis) dans le system prompt d'`agent.ts` (tous modes) — l'agent construit des apps avec DB/auth via `@supabase/supabase-js`, client `src/lib/supabase.js` lisant `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, SQL fourni à coller (RLS obligatoire), jamais de clé en dur, dégradation propre si clés absentes. **Sécurité** : `.env`/`.env.local` git-ignorés dans les projets (`SECRETS` dans `versions.ts`) ET exclus du zip (`index.ts`) — sinon les clés fuiteraient via le push GitHub (idée 16) ou l'export. @supabase/supabase-js non préinstallé (l'agent l'installe au besoin)
- **Contrat d'E/S / Coque Rigide (Phase Ultime jalon C)** : `server/src/contract.ts` + spec `docs/contrat-es.md`. `parseContract(raw)` transforme la sortie d'un modèle (balises `<mangoai>` write/edit/run/summary/axiom) en `ActionPlan` validé, ou `{ok:false,error}`. Format à BALISES (pas JSON) car les actions portent du code brut. Le modèle PROPOSE, MangoAI exécute. Sécurité : chemins project-relatifs only. Réparation avant rejet. **v1 = parser pur ; v2 LIVRÉE au Jalon D** : `executor.ts` (`executeContract`) applique le plan, `inspection.ts` juge, `eleve.ts` (`runRelay`) orchestre. La boucle Claude reste intacte (route séparée `model==="eleve"`)
- **Coque Souple (Phase Ultime jalon A)** : le system prompt est assemblé par `server/src/scenario.ts` (`assembleSystemPrompt(ctx)`) à partir de blocs nommés suivant un scénario (= mode), pas par une concaténation en dur. Les constantes de prompt (SYSTEM_APPEND, MODE_RULES, ANALYTIC_RULES, VISION_*, SUPABASE_RULES) vivent dans `scenario.ts`. `agent.ts` appelle juste `assembleSystemPrompt`. Ajouter/retirer un mode ou une capacité = modifier `SCENARIOS`/`BLOCKS`, pas le flux de `runAgent`. Couture `selectAxioms()` (axioms.ts) = point d'injection des axiomes ; **v2 LIVRÉE (Jalon D)** : sans contexte → registre complet (Claude inchangé) ; avec contexte `{task, max}` → axiomes pertinents plafonnés (Élève). Tout changement de prompt doit rester à comportement constant sauf intention explicite
- **Tests auto (idée 24, 2026-06-14)** : bloc `TESTS_RULES` dans `scenario.ts`, dans le scénario **elite uniquement** (gated comme analytic/plan). L'agent écrit des tests **Vitest** sur la **logique pure** (zéro config, pas de jsdom), met Vitest **à la demande** (`npm i -D vitest` + script `"test":"vitest run"` la 1ʳᵉ fois — PAS préinstallé dans `server/template/` pour que les projets sans test restent légers à l'export Phase B), lance `npx vitest run` (one-shot, jamais le watch) pour confirmer, reste proportionné (skip trivial/visuel), Playwright e2e seulement pour un flux critique. Prouvé par `test-scenario.ts` (gating : présent Élite / absent MVP ; verrouille aussi le gating analytic/vision/Figma de la Coque Souple). ⚠ Le template n'embarque toujours pas Vitest — c'est volontaire (install à la demande)
- **Mode MVP/Élite (idée 12)** : deuxième axe orthogonal au modèle. `ALLOWED_MODES`/`Mode` dans `agent.ts`, défaut `elite`. Le mode pilote `analytic = mode === "elite" && model !== "haiku"` (→ rituel analytique + thinking adaptatif), le jeu de règles vision (`VISION_RULES_ELITE` complet vs `VISION_RULES_MVP` allégé), et le budget vision (`setVisionContext(dir, url, mode)` → MVP `VISION_BUDGET_MVP` défaut 3, Élite `VISION_BUDGET` défaut 10). `mode` circule UI (localStorage `mangoai.mode`) → body `/api/chat` → `runAgent` → métriques. C'est l'interrupteur sur lequel brancher toute fonction future (idées 1, 2, 9, 11)
- **Vision** : le tool Read du preset claude_code lit nativement PNG/JPEG/PDF (paramètre pages, 20 max/appel) — les entrées visuelles passent par un fichier + chemin dans le prompt, JAMAIS par des blocs image dans le message utilisateur (un tool result se compacte, pas le message). Tool MCP in-process : `createSdkMcpServer` + `tool()` (zod), nom d'outil `mcp__<server>__<tool>`, `alwaysLoad: true` sinon différé derrière ToolSearch ; un bloc `{type:"image", data: base64, mimeType}` dans le retour du tool est VU par le modèle. Budget vision : env `VISION_BUDGET` (10/tour), reset + purge `.snapshots/` par `setVisionContext()` au début de chaque tour ; snapshots 1280×800 JPEG q80 ≈ 1 400 tokens chacun
- **Élève local / Ollama (Jalon D)** : `qwen2.5-coder:7b` via Ollama (`http://localhost:11434`, API compat OpenAI + `/api/chat`). **`OLLAMA_MODELS` redirigé sur `D:\ollama\models`** (variable User permanente — C: était presque plein). Démarrer le serveur Ollama : `ollama serve` (séparé du backend). Le 1er appel est lent (~60 s, chargement VRAM), les suivants ~5 s. Réglables : `ELEVE_MODEL`, `ELEVE_MAX_ATTEMPTS` (2), `ELEVE_AXIOM_CAP` (5)
- **Boucle de relais (Jalon D)** : `server/src/eleve.ts` → `runRelay(task, projectDir, opts, deps)`. L'Élève répond dans le contrat `<mangoai>` (prompt d'entrée STRICT obligatoire — sinon il dérape), `executeContract` applique, `inspectProject` (vrai `vite build`) juge **objectivement** ; après N échecs objectifs → escalade Claude qui corrige + écrit un axiome. Cerveaux `askEleve`/`escalate` **injectables** (`defaultRelayDeps` exporté) → testable sans réseau et réutilisé par l'Audit Scan (escalade neutralisée). ⚠ `relay.ts` existe déjà (error-relay des apps) — NE PAS confondre, le module de la boucle est `eleve.ts`
- **Exécuteur + inspection (Jalon D)** : `executor.ts` (`executeContract` — write atomique via `safe-io`, edit exact refusant find absent/ambigu, run sandboxé+timeout+liste noire, défense en profondeur sur chemins, arrêt à la 1ʳᵉ erreur) ; `inspection.ts` (`inspectProject` → signaux `ok|build-failed|timeout|no-deps|...`). **Escalade sur signaux OBJECTIFS uniquement**, jamais « le visuel a l'air bon »
- **Branchement chat (Jalon D)** : 4ᵉ cerveau `model === "eleve"` (sélecteur `Header.jsx`, `MODELS`) → `/api/chat` route vers `runRelay` (sinon `runAgent`/Claude inchangé), trace streamée en SSE, métriques `resolvedBy`/`attempts`/`projectType`
- **Discipline d'ablation (routine Jalon D)** : après CHAQUE nouvel axiome de code né d'une vraie escalade Élève→Claude, lancer `cd server && npx tsx src/audit-scan.ts --ablate` (suite AVEC vs SANS le dernier axiome). Si l'axiome **dégrade** l'Élève (build/1er tour en baisse) → l'amender ou le retirer (le clapet est anti-OUBLI, pas anti-correction). Tant qu'aucune escalade ne crée d'axiome de code, l'ablation n'a rien de neuf à tester. Le verdict d'ablation s'appuie sur le **rendement réel** (build+effet), pas la compilation seule. **Caveat n°7 — FERMÉ côté MESURE (2026-06-13)** : `vite build` ne juge que la compilation, pas l'*effet* d'un edit (un `<find>` raté = no-op → projet pristine qui build vert). Parade : `audit-verify.ts` (`verifyEffect`) + une attente `expect: EffectSpec` par tâche held-out → l'audit calcule le RENDEMENT RÉEL. **Démontré en vrai** : `edit-app-header` build 100 % mais rendement réel 89 % (escalade Claude a déclaré « résolu/build-vert » alors que le `<header>` n'a jamais atterri). ⚠ **Résidu** : le succès de `runRelay` EN PRODUCTION reste build-only (`inspection.ok`) — en open-ended pas d'effet attendu à comparer ; piste = exiger un diff non vide (statut.md piste n°8)
- **Audit Scan (Jalon D Phase 3)** : `server/src/audit-scan.ts` — **CLI, JAMAIS importé par `index.ts`** (production intacte). Suite **figée held-out** `audit-tasks.ts` (⚠ ne JAMAIS s'en servir pour extraire un axiome — sinon par-cœur). Empreintes (digest modèle + version Ollama + hash axiomes + suiteHash), journal `workspace/.audit.jsonl` (git-ignoré). `--ablate` = suite AVEC vs SANS le dernier axiome (`removeLastAxiom` d'axioms.ts) → mauvais pli causal ; **restaurer les octets BRUTS du registre** (pas `loadAxioms` qui trim) pour une restauration byte-identique. **Vérification d'EFFET (2026-06-13)** : `audit-verify.ts` (`verifyEffect`, pur) + `expect: EffectSpec` par tâche (`includes`/`includesAny`/`excludes` par fichier) → métrique **RENDEMENT RÉEL** (build ET changement atterri), distincte du build seul ; verdict d'ablation basé dessus. ⚠ `suiteHash` reste sur `id::prompt` (l'`expect` ne le change pas → comparaisons historiques de build préservées). Vérifier l'effet AVANT `cleanProject` (fichiers encore sur disque)
- **Relais clic → source (#5, pont pixel→code natif)** : `server/src/clicksource.ts`. ⚠ **`fiber._debugSource` a été RETIRÉ en React 19** (template = React 19.2 ; sondé empiriquement : seulement `_debugStack`/`_debugOwner`, pas de `{file,line}` propre) — l'hypothèse initiale était fausse. Solution native ET insensible aux versions : un **tampon Babel** stampe en DEV chaque élément hôte JSX avec `data-mango-src="fichier:ligne"` (Vite/Babel transforme déjà le JSX, zéro dépendance npm — un plugin Babel est juste une fonction). `ensureClickSourcePlugin(dir)` injecte le plugin **dans `vite.config.js`** (idempotent, ne touche que la forme `react()`, **neutralisé en prod via `NODE_ENV` → l'attribut ne fuit ni au build ni au déploiement**). Côté vivant : script `inspect-relay` injecté dans `index.html` par `ensureInspectRelay` (`relay.ts`, à côté de l'error-relay) — en mode inspection, un clic fait `elementFromPoint().closest('[data-mango-src]')` → `postMessage` au builder. UI : bouton ⌖ `MousePointerClick` dans `Preview.jsx` (postMessage `mangoai-builder` inspect-on/off vers l'iframe), `App.jsx` aiguille `inspect-pick` → précharge le composer (`seedInput`→Chat, SANS envoyer). `POST /api/inspect` lit l'extrait (`readSourceSnippet`). ⚠ effet de bord assumé : `vite.config.js` du projet généré est modifié (committé par `versions.ts`, ré-injecté à chaque preview) — dev-only/inoffensif. Prouvé : `test-clicksource.ts` (déterministe) + bancs live `exp-clicksource-{live,relay}.ts` → clic réel sur l'aperçu = `src/App.jsx:96` = `<span>` exact
- **Édition visuelle chirurgicale (#6)** : `buildVisualEditPrompt(projectDir, editTarget, userText)` (clicksource.ts) enrichit la tâche envoyée à l'agent avec le `fichier:ligne` EXACT + l'extrait (→ ligne marquée) + consigne d'edit CHIRURGICAL. Flux : la sélection clic→source arme `editTarget` ({src,tag,text}) dans `App.jsx`, `Chat.jsx` l'envoie dans le body `/api/chat` (consommée par message), `index.ts` construit un `agentPrompt` séparé (le `prompt` brut reste l'historique/le titre de version) passé à runAgent OU runRelay — **model-agnostic** (Claude ou Élève ; côté Élève, le chemin cité déclenche l'injection du contenu via #1). **Vérification d'effet en PROD** (parade caveat n°7, cas où la cible est connue) : `index.ts` capture les octets du fichier cible avant/après le tour → status `✓ Élément modifié` ou `⚠ fichier cible inchangé`. Prouvé e2e (`exp-visual-edit.ts`) : clic sur `<h1>Bella Napoli</h1>` → Élève SEUL ($0, 1er tour) remplace CE texte, voisin conservé, pas de réécriture — **le même edit qui échouait en aveugle (audit `edit-app-header`) réussit** car l'extrait rend le `<find>` copiable. La chaîne #1+#5+#6 converge

## Décisions prises
- Template React+Vite pré-copié dans `server/template/` (plus rapide que `npm create vite` à chaque projet) ; **Tailwind v4 préinstallé** (`@tailwindcss/vite` + `@import "tailwindcss"`) — les starters, eux, importent theme+utilities SANS preflight (leur CSS pur garde son rendu) ; règle : utilities pour le clonage de maquette, CSS pur pour les sites simples
- Streaming backend → frontend en SSE (pas de WebSocket, plus simple)
- TypeScript exécuté via `tsx` (pas de build step en dev)
- Limites mémoire en **caractères** (6 000 projet / 3 000 profil), pas en tokens — indépendant du modèle (pattern Hermes)
- Posture de la revue skills : **active** (« une passe qui ne sauve rien est une occasion manquée ») mais noms niveau classe et pas de skill pour les tours Q&A — calibré après essai
- Playwright en devDependency de l'UI (channel msedge) pour les vérifications headless ; jamais committer les captures
